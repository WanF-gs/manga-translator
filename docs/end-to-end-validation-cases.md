# 全流程验收用例集

> 版本: v1.0
> 验证范围: 三阶段全部产物与功能
> 验证人: 质量总监（最终签字）
> 验证日期: 2026-06-26

---

## 一、阶段一验收用例：技能集完整性

### 用例 A1-1: 技能文件数量验证

前置条件: 项目根目录为 demo_04
验证步骤:
1. 检查 skills/ 目录下是否存在 audit-closure-master.skills
2. 检查 skills/roles/ 目录下是否有15个 .skills 文件
预期结果: 主控技能 + 15个岗位子技能 = 16个文件全部存在
实际结果: 待验证
通过标准: 文件数量 = 16

### 用例 A1-2: 主控技能模块完整性

前置条件: skills/audit-closure-master.skills 存在
验证步骤:
1. 读取主控技能文件
2. 检查是否包含以下模块标题: 全局质量红线、全局终止条件、MCP工具强制规约、子Agent通用铁则、任务派发标准模板、迭代闭环全流程控制、技能加载触发机制
3. 检查9条质量红线是否全部定义 (R1-R9)
4. 检查8项终止条件是否全部定义 (T1-T8)
5. 检查5款MCP工具是否全部绑定（Firecrawl/GitHub/Neon/Playwright/SequentialThinking）
预期结果: 全部模块存在，9条红线8项条件5款工具无遗漏
实际结果: 待验证
通过标准: 模块清单 = 7/7, 红线 = 9/9, 条件 = 8/8, 工具 = 5/5

### 用例 A1-3: 岗位子技能内容完整性

前置条件: 15个岗位子技能文件存在
验证步骤:
1. 逐个读取每个子技能文件
2. 检查是否包含以下8个必备字段: 技能ID、岗位身份、所属组别、权责边界、绝对禁止项、MCP工具绑定表、交付物清单、任务完成判定标准
预期结果: 15个技能文件全部包含8个必备字段
实际结果: 待验证
通过标准: 15×8 = 120项全部满足

### 用例 A1-4: 规则一致性验证

前置条件: 原始15人团队文档存在
验证步骤:
1. 对比原始文档中的角色权责与技能文件中的定义
2. 逐条比对质量红线规则
3. 逐条比对终止条件规则
预期结果: 所有规则与原始文档100%一致，无简化
实际结果: 待验证
通过标准: 一致性 = 100%

---

## 二、阶段二验收用例：测试与巡检能力

### 用例 B2-1: 测试素材强制绑定

前置条件: 测试项目/ 目录存在且包含固定素材
验证步骤:
1. 读取 tests/e2e/webapp-testing.config.js
2. 检查 TEST_MATERIALS.root 是否硬编码为项目内的 测试项目/ 路径
3. 检查 images/pdf/archives 数组中是否均为 测试项目/ 目录下的文件
4. 尝试在测试中引用外部素材路径，验证是否被拒绝
预期结果: 配置中所有素材路径均为 测试项目/ 目录内文件
实际结果: 待验证
通过标准: 素材路径100%来自 测试项目/ 目录

### 用例 B2-2: 测试用例模板标准化

前置条件: tests/e2e/case-template.md 存在
验证步骤:
1. 检查模板是否包含 PRD条款编号 字段
2. 检查模板是否包含 测试素材路径 字段（强制来自 测试项目/）
3. 检查模板是否包含 四类测试场景（正向/异常/边界/误操作）
4. 检查模板是否包含 操作步骤表格 + 预期断言表格 + 测试结果记录表格
预期结果: 模板字段完整，强制字段已标注
实际结果: 待验证
通过标准: 模板字段完整性 = 100%

### 用例 B2-3: 一键回归脚本可用性

前置条件: tests/e2e/run-all.sh 存在，前端服务可启动
验证步骤:
1. 执行 bash tests/e2e/run-all.sh --layer L1
2. 检查前置校验是否通过（素材目录存在性检查）
3. 检查是否输出测试汇总报告
4. 检查 reports/test/ 目录是否生成报告文件
预期结果: 脚本正常执行，前置校验通过，报告已生成
实际结果: 待验证
通过标准: 脚本退出码=0，reports/test/ 有新生成的报告文件

### 用例 B2-4: 进度看板数据同步

前置条件: Neon 数据库连接正常，agent_task_status 表存在
验证步骤:
1. 在 Neon SQL Editor 中执行 data/progress-dashboard.sql 创建视图
2. 查询 SELECT * FROM v_global_overview
3. 插入测试数据到 agent_task_status
4. 再次查询，验证数据实时同步
预期结果: 视图创建成功，查询结果与表中数据一致
实际结果: 待验证
通过标准: 视图创建无错误，数据查询与插入一致

### 用例 B2-5: 迭代简报自动生成

前置条件: scripts/generate-iteration-report.js 存在
验证步骤:
1. 执行 node scripts/generate-iteration-report.js --phase phase-0
2. 检查 reports/audit/ 目录是否生成简报文件
3. 检查简报是否包含六大板块: 执行概览/各组别完成情况/通过率变化/风险项/终止条件判断/下一步行动
预期结果: 简报生成成功，六大板块全部存在
实际结果: 待验证
通过标准: 报告文件存在且内容包含所有六大板块标题

---

## 三、阶段三验收用例：代码级闭环管控

### 用例 C3-1: 子Agent无法绕过提交步骤直接终止

前置条件: sdk/agent-lifecycle-hooks.js 已加载
验证步骤:
1. 创建 Agent 实例
2. 在未完成 GitHub 提交和 Neon 更新时调用 onTerminate
3. 检查返回值中 canTerminate 是否为 false
4. 检查 errors 数组是否包含 GitHub 和 Neon 的错误信息
预期结果: onTerminate 返回 canTerminate=false，并给出明确的补全要求
实际结果: 待验证
通过标准: canTerminate=false, errors.length > 0

### 用例 C3-2: 角色漂移自动拦截

前置条件: sdk/agent-lifecycle-hooks.js 已加载
验证步骤:
1. 创建后端开发 engineer Agent 实例
2. 调用 onOperation 执行 'test' 类型操作（在 forbiddenOperations 中）
3. 检查返回值是否为 false
4. 连续3次调用 forbidden 操作
5. 检查第3次后 driftCount 是否触发终止
预期结果: 单次拦截成功，连续3次后触发终止
实际结果: 待验证
通过标准: 第1次返回 false, 第3次后 driftCount >= 3

### 用例 C3-3: Agent失活自动接管

前置条件: sdk/task-state-machine.js 已加载
验证步骤:
1. 创建任务状态机实例
2. 创建一个 IN_PROGRESS 状态的任务
3. 手动触发 _patrol() 3次（模拟连续催办）
4. 检查任务状态是否变为 PENDING_TAKEOVER
预期结果: 连续2次催办无响应后，任务自动标记为待接管
实际结果: 待验证
通过标准: 第3次巡检后任务状态 = 待接管

### 用例 C3-4: 非法状态转换被拒绝

前置条件: sdk/task-state-machine.js 已加载
验证步骤:
1. 创建任务（状态 = PENDING_DISPATCH）
2. 尝试直接从 PENDING_DISPATCH 转换为 APPROVED（非法跳过 IN_PROGRESS 和 SUBMITTED）
3. 检查是否抛出异常
预期结果: 状态转换被拒绝，抛出错误信息
实际结果: 待验证
通过标准: 抛出 Error 且消息包含 "非法的状态转换"

### 用例 C3-5: 交付物提交网关五重校验

前置条件: sdk/unified-submit-gateway.js 已加载
验证步骤:
1. 创建一个缺少必填字段的提交
2. 创建一个使用非法路径的提交
3. 创建一个内容格式不符合要求的提交
4. 创建一个引用外部素材的提交
5. 创建一个未绑定 PRD 编号的提交
6. 对以上5个提交分别调用 quickValidate
预期结果: 5个提交全部被拒绝，返回明确错误原因
实际结果: 待验证
通过标准: 5个 quickValidate 结果均为 passed=false

### 用例 C3-6: 阶段完成后自动推进

前置条件: sdk/task-state-machine.js 已加载
验证步骤:
1. 创建3个 phase-0 的任务
2. 将3个任务全部审批通过
3. 检查是否触发 phaseCompleted 事件
预期结果: 阶段全部验收通过后自动触发下一阶段
实际结果: 待验证
通过标准: phaseCompleted 事件被触发

### 用例 C3-7: 全流程编排完整闭环

前置条件: sdk/workflow-orchestrator.js 已加载
验证步骤:
1. 创建编排器实例
2. 调用 orchestrator.run()
3. 监控整个执行过程: 第0阶段 → 迭代 → 最终验收
4. 检查最终返回值中的 completed 是否为 true
预期结果: 全流程自动完成，无需人工干预
实际结果: 待验证
通过标准: 返回值 completed=true, iterations > 0

---

## 四、综合验收用例：跨阶段集成

### 用例 D4-1: 端到端提交流程

前置条件: 三阶段全部SDK已就绪
验证步骤:
1. 启动全流程编排器
2. 模拟一个完整的"创建任务→执行→提交→验收"流程
3. 跟踪任务在状态机中的完整生命周期
4. 验证 GitHub + Neon 双提交完成
预期结果: 任务从 待派发 经 执行中 → 已提交 → 验收通过 完整流转
实际结果: 待验证
通过标准: 任务最终状态 = 已完成

### 用例 D4-2: 连续3轮迭代无角色漂移

前置条件: 全流程编排运行中
验证步骤:
1. 运行3轮完整迭代
2. 检查各 Agent 实例的 driftCount
3. 检查是否有跨岗操作被记录的警告
预期结果: 3轮迭代中所有 Agent 的 driftCount = 0
实际结果: 待验证
通过标准: 所有 Agent.driftCount = 0

### 用例 D4-3: 无流程中断验证

前置条件: 全流程编排运行中
验证步骤:
1. 运行完整流程
2. 检查是否有流程中断记录
3. 检查是否有待接管任务在超时5分钟内被接管
预期结果: 流程无中断，接管机制响应时间 < 5分钟
实际结果: 待验证
通过标准: 中断次数 = 0

### 用例 D4-4: 无交付物遗漏验证

前置条件: 全流程编排运行完成
验证步骤:
1. 对比 PRD 对标清单中的所有条款
2. 检查 deliverables/ 目录下每个阶段的交付物
3. 验证 reports/ 目录下报告完整性
预期结果: 所有要求的交付物全部就位
实际结果: 待验证
通过标准: 交付物遗漏 = 0

---

## 五、验收签署

| 角色 | 签署状态 | 签署时间 | 备注 |
|------|---------|---------|------|
| 项目总监 | 待签署 | - | 阶段一/二/三全部验收通过后签字 |
| 质量总监 | 待签署 | - | 需逐项验证32个验收用例 |

签署完成后，本项目三阶段全部落地完成，进入日常迭代维护。

---

> **本验收用例集为最终验收依据，所有用例必须逐项通过，不得跳过。**
