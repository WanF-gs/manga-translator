# 多 Agent 测试审计闭环 - 总控技能包

> 技能ID: audit-closure-master
> 版本: v2.0
> 适用范围: 漫画多语言智能翻译与图像合成系统全生命周期
> 加载策略: 项目级自动加载（打开本项目的任何子Agent自动加载）
> 更新说明: v2.0 增补全局黑盒隔离规则、固定测试素材强制规则、MCP/插件工具全量强制绑定规则、跨岗信息防火墙、违规触发机制，所有新增规则优先级与质量红线同级。

---

## 一、全局质量红线（9条）

以下9条红线为强制执行标准，任何任务产出触及任一条红线，该任务自动标记为「驳回重传」：

| # | 红线规则 | 检测方式 | 触发动作 |
|---|---------|---------|---------|
| R1 | 交付物未提交至GitHub仓库 `deliverables/` 对应目录 | 提交网关校验 | 驳回+记录 |
| R2 | Neon数据库任务状态未更新为「已提交」| 数据库轮询 | 禁止终止 |
| R3 | 测试用例未引用项目内 `测试项目/` 目录固定素材 | 路径硬编码校验 | 测试无效 |
| R4 | 跨岗位操作（执行非本岗位权责范围的任务）| 角色锚定钩子检测 | 拦截+唤醒 |
| R5 | E2E测试未映射PRD条款编号 | 用例模板校验 | 驳回重写 |
| R6 | 交付物格式不符合对应岗位规范 | 提交网关校验 | 驳回+要求 |
| R7 | 连续3轮迭代后仍有P0未修复项 | 巡检引擎检测 | 终止迭代 |
| R8 | 全量回归测试通过率低于85% | 测试报告解析 | 禁止验收 |
| R9 | P1测试通过率低于98% | 测试报告解析 | 禁止验收 |

---

## 二、全局终止条件（8项）

以下8项全部满足时，方可进入最终验收：

| # | 终止条件 | 验证方式 | 阻塞级别 |
|---|---------|---------|---------|
| T1 | P0功能100%实现且测试100%通过 | PRD对标清单+测试报告 | 必须 |
| T2 | P1功能100%实现且测试通过率≥98% | PRD对标清单+测试报告 | 必须 |
| T3 | P2功能≥95%实现 | PRD对标清单 | 必须 |
| T4 | 连续3轮迭代无新增P0/P1问题 | 问题追踪日志 | 必须 |
| T5 | 性能指标全部达标（API响应P50≤100ms, P99≤500ms） | 性能测试报告 | 必须 |
| T6 | 合规检查项全部通过（安全审计+边界审计） | 合规审计报告 | 必须 |
| T7 | 双官签署（项目总监+质量总监） | 签署记录 | 必须 |
| T8 | 全部交付物已归档至GitHub `deliverables/` 对应目录 | 仓库校验 | 必须 |

---

## 三、MCP工具强制规约

### 3.1 工具-场景强制绑定

以下5款MCP工具已与本项目强制绑定，任何任务执行必须遵守对应使用规则：

#### Firecrawl MCP Server
- **强制绑定场景**: 外部API文档检索、第三方服务接口变更监控
- **禁止场景**: 不得用于抓取内部代码库、不得用于抓取竞品信息
- **强制规则**: 所有外部技术调研结论必须附带Firecrawl抓取的原始文档URL
- **无工具记录结论无效**: 声称基于"检索结果"但无Firecrawl调用记录时，结论视为无效

#### GitHub MCP Server
- **强制绑定场景**: 交付物提交、代码变更追踪、PR审查
- **强制规则**: 所有交付物必须通过GitHub MCP提交（`create_or_update_file`/`push_files`）
- **提交路径规范**: `deliverables/phase-{N}/{role-name}/` 格式
- **无工具记录结论无效**: 声称"已提交代码"但无GitHub MCP调用记录时，视为未提交

#### Neon MCP Server
- **强制绑定场景**: 任务状态更新、进度查询、数据校验
- **强制规则**: 每个子Agent任务开始时写入 `agent_task_status` 表（状态=执行中），完成时更新（状态=已提交）
- **无工具记录结论无效**: 声称"已完成任务"但Neon数据库中对应记录状态未更新时，视为未完成

#### Playwright MCP Server
- **强制绑定场景**: 前端E2E浏览器测试、页面截图验证
- **强制规则**: 所有E2E测试必须使用 `测试项目/` 目录素材，测试完成后必须截图保存至 `reports/test/screenshots/`
- **无工具记录结论无效**: 声称"前端功能正常"但无Playwright截图记录时，结论无效

#### SequentialThinking MCP Server
- **强制绑定场景**: 子Agent启动锚定、每3次操作角色唤醒、复杂问题分解
- **强制规则**: 每个子Agent启动时必须调用一次SequentialThinking完成角色身份锚定
- **无工具记录结论无效**: 跳过启动锚定的Agent实例，其全部产出标记为「待审计」

#### webapp-testing 插件
- **强制绑定场景**: 测试环境初始化、用例模板管理、测试环境一致性保证
- **强制规则**: 统一使用webapp-testing插件管理测试环境与用例模板，禁止私自配置测试环境
- **禁止场景**: 不得绕过插件直接操作测试环境、不得自行创建非标准测试环境配置
- **无工具记录结论无效**: 未通过webapp-testing插件启动的测试环境，其测试结果不计入正式验收

#### data 插件
- **强制绑定场景**: 进度看板生成、性能统计、迭代简报
- **强制规则**: 所有数据必须来自Neon数据库与真实测试日志，禁止主观编造数据
- **禁止场景**: 不得基于估计值生成统计报告、不得修改原始数据后输出
- **无工具记录结论无效**: 无data插件生成的统计报告，仅凭人工汇总的数据视为参考，不计入正式交付

### 3.2 通用工具使用铁则

1. 任何产出结论必须有对应MCP工具调用记录支撑
2. 工具调用失败时必须记录错误日志，不得静默跳过
3. 所有MCP工具调用必须在对话中可见（不得隐藏调用）
4. 无工具记录的操作不计入验收，产出视为无效

---

## 四、子Agent通用铁则

### 4.1 启动锚定协议（on-init）

每个子Agent实例启动时，必须执行以下3步：

1. **调用SequentialThinking**: 确认角色身份、本阶段目标、交付物要求
2. **加载岗位技能包**: 读取对应 `skills/roles/{role-id}.skills` 文件
3. **写入开始状态**: 通过Neon MCP向 `agent_task_status` 插入「执行中」记录

锚定输出格式：
```
[角色锚定] 我是{角色名称}({角色ID})，属于{组别}组。
本阶段任务: {任务描述}
交付物路径: deliverables/phase-{N}/{role-id}/
提交路径: GitHub仓库对应目录 + Neon状态更新
核心规则: {岗位核心约束}
```

### 4.2 中途心跳钩子（on-operation）

每执行3次核心操作（文件读取/代码修改/分析操作），必须自动触发：

1. **角色唤醒**: 输出一行角色身份确认语句
2. **规则校验**: 检查当前操作是否在岗位权责范围内
3. **方向确认**: 确认当前操作方向未偏离任务目标

唤醒锚定输出格式：
```
[心跳唤醒] 角色{角色ID} | 已完成{count}次操作 | 当前目标: {sub-goal} | 下一步: {next-action}
```

若检测到角色漂移（跨岗操作），立即输出：
```
[角色漂移警告] 检测到跨岗操作: {违规内容}。当前角色{role}无权限执行{target_role}的职责。立即切换到正确规则。
```

### 4.3 双提交闭环协议（on-terminate）

子Agent任务完成前，必须同时满足以下两个条件，否则禁止终止：

| 条件 | 验证方式 | 不满足时的行为 |
|------|---------|---------------|
| **GitHub提交成功** | 调用GitHub MCP `push_files` 或 `create_or_update_file`，返回成功日志 | 强制重新提交，最多重试3次 |
| **Neon状态更新** | 调用Neon MCP `run_sql` 更新 `agent_task_status` 对应记录状态为「已提交」 | 强制更新，记录失败原因 |

终止确认输出格式：
```
[提交闭环确认]
✅ GitHub: {文件路径} 已推送至 {branch}，commit: {hash}
✅ Neon: agent_task_status.{task_id} 状态已更新为「已提交」
⏱ 执行时长: {duration}
📋 产出清单: {deliverables_list}
```

### 4.4 异常降级规则

| 异常类型 | 检测条件 | 自动动作 |
|---------|---------|---------|
| 子Agent失活 | 连续2次催办（间隔5分钟）无状态更新 | 自动标记为「待接管」，通知同组备用角色 |
| 提交失败 | GitHub/Neon操作连续3次失败 | 记录错误日志，通知项目监理 |
| 任务超时 | 执行时间超过maxDurationMinutes | 自动标记为异常，纳入催办队列 |

---

## 五、任务派发标准模板

### 5.1 任务派发格式

```markdown
## 任务派发单

**派发人**: 项目总监
**承接角色**: {role_name}({role_id})
**所属组别**: {group_name}
**任务编号**: {task_id}
**阶段**: Phase-{N}
**优先级**: P{0-3}

### 任务描述
{具体任务内容，含PRD条款引用}

### 交付物要求
- 文件列表: {file_list}
- 提交路径: deliverables/phase-{N}/{role-id}/
- 格式要求: {format_spec}

### 成功标准
1. GitHub提交成功（路径正确）
2. Neon状态更新为「已提交」
3. {role-specific criteria}

### 素材/依赖
- 测试素材: 测试项目/{path}
- 前置依赖: {dependencies}
```

### 5.2 回执巡检规则

| 巡检点 | 检查内容 | 异常动作 |
|--------|---------|---------|
| 派发后 2分钟 | 任务状态是否已更新为「执行中」 | 未更新则发送催办 |
| 派发后 10分钟 | 是否有心跳唤醒记录 | 无记录则发送规则重申 |
| 派发后 30分钟 | 任务是否已完成闭环 | 未完成则发送第2次催办 |
| 第2次催办后 5分钟 | 任务状态是否有更新 | 无更新则标记「待接管」 |

---

## 六、迭代闭环全流程控制

### 第0阶段：基准盘点
1. PRD首席合规官 → 生成PRD全量对标清单
2. 业务边界审计师 → 生成边界审计报告
3. 测试架构首席专家 → 生成测试设计方案
4. 自动化测试工程师 → 执行首轮全量测试
5. 项目总监 → 汇总问题，生成优先级队列

### 迭代阶段（第N轮）
1. 项目总监 → 按优先级派发修复任务
2. 开发/修复工程师 → 实现/修复
3. 代码审查专家 → 代码审查
4. 自动化测试工程师 → 单点验证
5. 回归测试专员 → 全量回归
6. 项目监理 → 生成迭代简报，判断终止条件
7. 若终止条件未满足 → 进入第N+1轮

### 最终验收阶段
1. 质量总监 → 全量验收
2. 数据与合规专家 → 最终合规审计
3. 项目总监 + 质量总监 → 双签验收结论
4. 全部交付物归档至GitHub

---

## 七、技能加载触发机制

| 触发级别 | 触发条件 | 加载内容 |
|---------|---------|---------|
| 项目级 | 打开漫画翻译项目工作区 | 自动加载本总控技能 |
| 任务级 | 子Agent承接对应岗位任务 | 自动加载对应岗位子技能 |
| 操作级 | 每执行3次核心操作 | 自动唤醒岗位核心规则校验 |

---

## 八、全局测试素材铁则（最高强制级，优先级与质量红线同级）

### 8.1 唯一合法素材目录

项目根目录下 `测试项目/` 目录为唯一合法测试素材来源。该目录为只读目录，内部结构：

```
测试项目/   ← 项目内固定测试素材目录（只读）
├── *.jpg    ← 36张测试用漫画图片
├── *.pdf    ← 9个测试用PDF文件
└── *.rar    ← 1个测试用压缩包
```

绝对禁止规则：
1. 禁止任何角色增删改 `测试项目/` 目录内任何文件
2. 所有上传类、导入类、解析类、翻译类测试，必须100%引用该目录下的原生文件
3. 禁止使用外部素材、AI生成素材、自定义模拟素材
4. 禁止修改素材格式、裁剪素材内容、伪造素材数据，所有测试必须基于原始素材文件执行
5. 测试用例审计师必须核验素材路径，非指定目录素材的测试结果一律作废，不计入通过率

### 8.2 素材违规判定标准

| 违规类型 | 判定条件 | 处罚 |
|---------|---------|------|
| 路径违规 | 测试用例引用的素材路径不包含 `测试项目/` 前缀 | 该用例结果作废 |
| 格式篡改 | 上传的素材文件MD5与 `测试项目/` 内原始文件不一致 | 该用例结果作废，记录违规 |
| AI生成素材 | 使用AI工具生成的模拟测试图片/文件 | 本轮所有测试结果作废 |
| 外部素材 | 使用非 `测试项目/` 目录下的任何文件 | 本轮所有测试结果作废 |

---

## 九、MCP与插件强制规约（全量工具-场景强制绑定）

### 9.1 强制工具集

以下7款MCP工具和2款插件为本项目强制工具集，核心操作必须通过对应工具执行：

| # | 工具/插件 | 类型 | 强制绑定场景 |
|---|----------|------|-------------|
| 1 | Firecrawl MCP | MCP Server | PRD原文抓取、外部文档检索 |
| 2 | Playwright MCP | MCP Server | 页面级测试、E2E全链路浏览器操作 |
| 3 | Neon MCP | MCP Server | 任务状态同步、进度查询、数据校验 |
| 4 | GitHub MCP | MCP Server | 交付物提交、代码变更追踪 |
| 5 | SequentialThinking | MCP Server | 任务启动锚定、角色身份校验 |
| 6 | webapp-testing | 插件 | 测试环境管理、用例模板标准化 |
| 7 | data | 插件 | 进度看板、性能统计、迭代简报生成 |

### 9.2 工具-场景强制绑定细则

#### Firecrawl MCP Server
- 强制绑定场景: PRD对标与功能拆解，必须使用Firecrawl抓取PRD原文
- 强制规则: 所有验收标准以PRD原文为准，禁止凭记忆、主观解读判定功能合规性
- 禁止场景: 不得用于抓取内部代码库
- 无工具记录结论无效: 声称"已对标PRD条款"但无Firecrawl抓取记录时，结论无效

#### Playwright MCP Server
- 强制绑定场景: 页面级测试、E2E全链路测试
- 强制规则: 必须在真实浏览器中执行操作，测试完成后截图保存至 `reports/test/screenshots/`
- 禁止场景: 不得使用纯接口调用、纯代码静态分析替代浏览器操作
- 无工具记录结论无效: 无Playwright截图记录的测试结论一律无效，不计入覆盖率

#### Neon MCP Server
- 强制绑定场景: 任务状态变更、问题录入、进度更新
- 强制规则: 所有任务状态变更必须同步至 `agent_task_status` 表，仅对话内口头更新无效
- 无工具记录结论无效: 声称"任务已完成"但Neon中状态未同步时，视为未完成

#### GitHub MCP Server
- 强制绑定场景: 交付物提交、归档
- 强制规则: 所有交付物必须通过统一提交网关归档至GitHub对应目录，同步更新Neon状态
- 无工具记录结论无效: 本地文件、对话内输出不算正式交付

#### SequentialThinking MCP Server
- 强制绑定场景: 任务启动锚定
- 强制规则: 所有子岗位承接任务第一步必须调用SequentialThinking，完成身份、权责、交付物、规则的六项锚定
- 锚定六项: 角色身份、所属组别、本阶段任务、交付物路径、提交路径、核心约束规则
- 无工具记录结论无效: 未通过SequentialThinking校验开工的任务，产出标记为「待审计」

#### webapp-testing 插件
- 强制绑定场景: 测试环境初始化、用例模板管理
- 强制规则: 统一使用webapp-testing插件管理测试环境与用例模板，保证环境一致性
- 禁止场景: 禁止私自配置测试环境、禁止绕过插件直接操作测试环境

#### data 插件
- 强制绑定场景: 进度看板生成、性能统计、迭代简报
- 强制规则: 所有数据必须来自Neon数据库与真实测试日志
- 禁止场景: 禁止主观编造数据、禁止基于估计值生成统计报告

### 9.3 通用铁则

1. 任何核心操作必须有对应MCP/插件工具调用记录支撑
2. 工具调用失败时必须记录错误日志，不得静默跳过
3. 所有MCP/插件调用必须在对话中可见（不得隐藏调用）
4. 无工具记录的操作不计入验收，产出视为无效

---

## 十、全局黑盒权责隔离原则

### 10.1 信息物理隔离

测试与开发信息物理隔离，分为三个独立信息域：

| 信息域 | 可访问角色 | 核心规则 |
|--------|-----------|---------|
| 测试域 | 测试与质量组所有角色 | 仅可访问PRD功能清单、页面操作结果、测试素材 |
| 开发域 | 开发实现组所有角色 | 仅可访问业务源代码、问题描述文档、复现步骤 |
| 管控域 | 管控与审计组所有角色 | 全目录可读，仅用于合规审计，不得替代岗位输出 |

### 10.2 隔离执行规则

1. 测试岗不得读取业务源代码（src/、app/、server/、api/、components/、internal/、cmd/、services/ 等所有包含业务逻辑实现的目录）
2. 开发岗不得查看测试用例详情与断言逻辑（tests/、e2e/ 目录下的测试脚本源码）
3. 功能判定唯一标准: 功能是否实现、是否可用，仅以「真实浏览器页面操作结果」为依据，代码文件存在、接口定义存在均不构成功能完成的判定依据
4. 问题传递单向化: 测试只输出「现象 + 复现步骤 + 预期效果」，开发只输出「修复结果 + 验证说明」，禁止跨岗讨论实现细节

### 10.3 跨岗信息防火墙

| 岗位组别 | 允许访问范围 | 绝对禁止访问 |
|----------|--------------|--------------|
| 测试与质量组 | tests/ 目录、测试项目/ 素材目录、项目访问地址、测试账号、PRD 用户功能清单、reports/test/ 目录 | 所有业务源代码目录（src/、app/、server/、api/、components/、internal/、cmd/、manga-translator-backend/ 下各服务实现代码）、数据库表结构定义（database/ddl/）、接口实现代码 |
| 开发实现组 | 业务源代码目录、问题描述文档、复现步骤、deliverables/ 目录 | 测试用例源码（tests/、e2e/）、断言逻辑、测试脚本内部实现、测试用例详情 |
| 管控与审计组 | 全目录可读，仅用于合规审计 | 不得直接修改代码、不得直接编写测试用例、不得替代岗位输出 |

---

## 十一、违规触发机制

### 11.1 违规定义与处罚分级

| 违规级别 | 违规行为 | 处罚 |
|---------|---------|------|
| 一级违规 | 使用非指定目录素材 | 本轮所有测试结果作废，违规记录写入Neon |
| 一级违规 | 测试岗读取业务源代码并用于功能判定 | 视为测试放水，本轮所有测试结论全部作废 |
| 一级违规 | 核心操作未使用对应MCP工具 | 视为无效操作，不计入验收 |
| 二级违规 | 零问题报告（单旅程问题数少于2个） | 默认放水，打回重测 |
| 二级违规 | 无凭证输出"测试通过"结论 | 该结论作废，补充凭证后重审 |

### 11.2 累计违规处理

违规记录同步写入Neon `agent_task_status` 表：
- 累计1次违规: 任务标记为「违规警告」，本轮测试结果纳入审计复核
- 累计2次违规: 触发质量红线回退机制，代码回退至上一全量通过版本，该轮所有测试结果全部作废
- 累计3次违规: 暂停该岗位操作权限，通知项目总监人工介入

### 11.3 违规检测触发点

| 检测点 | 检测方式 | 检测频率 |
|--------|---------|---------|
| 素材路径校验 | 审计师扫描测试报告中的所有文件引用路径 | 每轮测试后 |
| 跨岗访问校验 | 子Agent操作日志中读取业务代码的记录扫描 | 实时监控 |
| MCP工具使用校验 | 检查关键操作是否有对应MCP调用记录 | 每轮任务闭环时 |
| 凭证完整性校验 | 审计师核查每条用例的截图与日志 | 每轮测试后 |

---

> **本技能为总控级，所有子Agent和子技能均以此为最高权威规则来源。任何子技能与本技能冲突时，以本技能为准。**
